// COMANDOS EJECUTADOS
// => tsc test.ts
// => node test.js
// => tsc --watch o - w + file.ts para compilar un solo archivo
// => tsc --init crear archivo tsconfig.json
// => tsc para compilar varios archivos usando el tsconfig.json
// => tsc && .ts file ejecutar para compilar y mostrar el resultado

console.log('hola mundo!!!');
let nombre:string = 'luis';
let cantidadEstudiantes:number = 50;

// backtick
// Cadena multilinea
let frase = `
    Una frase de prueba
    Primera linea
    Segunda linea
    Tercera linea
    Quinta linea
`;

console.log(frase);